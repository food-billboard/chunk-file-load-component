import React, { memo } from 'react';
import { ContainerProps } from '../index';

const ViewListFileContainer = memo((props: ContainerProps) => {
  return <div></div>;
});

export default ViewListFileContainer;
